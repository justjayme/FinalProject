<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="wit.ie.finalproject">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/appicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SidesActivity" />
        <activity android:name=".Sides1Activity" />
        <activity android:name=".DinnerActivity" />
        <activity android:name=".Dinner1Activity" />
        <activity android:name=".DessertsActivity" />
        <activity android:name=".Desserts1Activity" />
        <activity android:name=".EditDataActivity" />
        <activity android:name=".ListDataActivity" />
        <activity android:name=".DatabaseHelper" />
        <activity android:name=".DinnerDatabaseHelper" />
        <activity android:name=".DinnerEditDataActivity" />
        <activity android:name=".DinnerListDataActivity" />
        <activity android:name=".DessertsDatabaseHelper" />
        <activity android:name=".DessertsEditDataActivity" />
        <activity android:name=".DessertsListDataActivity" />
        <activity android:name=".SidesDatabaseHelper" />
        <activity android:name=".SidesEditDataActivity" />
        <activity android:name=".SidesListDataActivity" />
        <activity android:name=".MyCameraActivity" />
        <activity android:name=".EditActivity" />


        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
    </application>

</manifest>